//更改方法

export const saveTitle = (state, title) => {
  state.Title = title
}

export const saveType = (state, type) => {
  state.Type = type
}

export const saveArticleId = (state, id) => {
  state.ArticleId = id
}

export const savePage = (state, page) => {
  state.Page = page
}

export const saveFlag = (state, flag) => {
  state.Flag = flag
}

